name: Build Windows App

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - "time-tracker-app/**"
      - "time-tracker-app/.github/workflows/windows-tauri-build.yml"

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: time-tracker-app

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: time-tracker-app/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Install frontend deps
        run: npm ci

      - name: Build Tauri (Windows)
        run: npm run tauri:build:win

      - name: Upload MSI
        uses: actions/upload-artifact@v4
        with:
          name: time-tracker-windows-msi
          path: time-tracker-app/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/msi/*.msi
          if-no-files-found: ignore

      - name: Upload NSIS EXE
        uses: actions/upload-artifact@v4
        with:
          name: time-tracker-windows-nsis
          path: time-tracker-app/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe
          if-no-files-found: ignore
